session: internvl_frozen

backbone:
  name: internvl
  embedding_dim: 2048
  dropout: 0.0
  pool: visual_mean

loss:
  name: infonce
  temperature: 0.07

training:
  epochs: 0
  batch_size: 8
  lr: 0.0
  warmup_epochs: 0
  num_workers: 4
  mixed_precision: bf16
  eval_every: 1
  eval_batch_size: 8
