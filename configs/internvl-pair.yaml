session: internvl_pair_frozen

backbone:
  name: internvl_pair
  embedding_dim: 0
  dropout: 0.0

loss:
  name: infonce
  temperature: 0.07

training:
  epochs: 0
  batch_size: 8
  lr: 0.0
  warmup_epochs: 0
  num_workers: 4
  mixed_precision: bf16
  eval_every: 1
