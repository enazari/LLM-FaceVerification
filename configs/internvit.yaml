session: internvit_frozen

backbone:
  name: internvit
  embedding_dim: 1024
  dropout: 0.0
  pool: mean

loss:
  name: infonce
  temperature: 0.07

training:
  epochs: 0
  batch_size: 200
  lr: 0.0
  momentum: 0.9
  weight_decay: 0.0
  warmup_epochs: 0
  num_workers: 6
  mixed_precision: fp16
  eval_every: 1
